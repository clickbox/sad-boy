{
  "name": "phaser_v3",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "parcel build src/index.html",
    "start": "parcel ./src/index.html --host $IP --port $PORT"
  },
  "devDependencies": {
  },
  "dependencies": {
    "phaser": "^3.10.1"
  }
}
